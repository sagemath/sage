// See https://aka.ms/devcontainer.json for format details.
{
    "name": "gentoo overlay 'sage-on-gentoo' downstream Sage (≥ 8-core)",
    "dockerFile": "Dockerfile",
    "onCreateCommand": "echo 'Edit .devcontainer/downstream-gentoo-overlay.json (updateContentCommand) to run project-specific startup commands'",
    // There's no SAGE_LOCAL, so remove the symlink 'prefix'.
    "updateContentCommand": "rm -f prefix && ln -sf /usr venv",
    "extensions": [
        "ms-python.python"
    ]
}
